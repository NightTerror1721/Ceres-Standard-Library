-- list: empty
-- list: order
-- list: remove
-- list: pop
-- list: removing while walking
-- vector: push and access
-- vector: copy in, pop, clear
-- vector: insert and remove
-- vector: sort
-- vector: reserve
-- vector: structs
-- ring: fill and drain
-- ring: wrapping
-- ring: bytes 128 and up come back unsigned
-- ring: blocks and clear
-- ring: a size that is not a power of two rounds down
-- map: basics
-- map: replace, NULL values
-- map: the key is copied
-- map: remove
-- map: growing
-- map: each
-- map: remove many, then reuse
-- map: clear
-- map: keys that collide
-- pqueue: order
-- pqueue: a max queue, and popping without taking the value
-- pqueue: records and a long random run
-- bitset: single bits
-- bitset: finding a free bit
-- bitset: set all, and sizes at a word boundary
-- strbuf: appending
-- strbuf: printf-style
-- strbuf: growing a long way
-- strbuf: taking the memory
-- slist: empty
-- slist: push front and walk
-- slist: remove_after and pop_front
-- slist: removing while walking
-- stack: empty
-- stack: LIFO order
-- deque: empty
-- deque: pushing both ends keeps logical order
-- deque: popping both ends
-- deque: grows past its initial capacity, wrapping included
-- cqueue: capacity rounds down to a power of two
-- cqueue: fills, empties, wraps
-- cqueue: clear drops what was waiting
-- dsu: starts with every element its own group
-- dsu: union merges groups transitively
-- dsu: union of an already-connected pair does nothing odd
-- flatmap: empty
-- flatmap: set keeps pairs sorted by key
-- flatmap: get and replace
-- flatmap: remove
-- flatset: add, has, remove
-- bloom: a never-added key is reported absent
-- bloom: an added key is always reported present
-- bloom: clear forgets everything
-- gmap: empty
-- gmap: set, get, replace
-- gmap: remove, growing back and forth
-- gmap: string keys via a key that is itself a pointer
-- hset: add, has, remove
-- multimap: empty key has nothing
-- multimap: several values on one key, most recent first
-- multimap: each visits every value
-- multimap: remove one match, the rest survive
-- multimap: removing every value drops the key entirely
-- slotmap: empty
-- slotmap: insert and read back by handle
-- slotmap: a removed handle goes stale, even if its slot is reused
-- slotmap: removing the same handle twice is harmless
-- iheap: empty
-- iheap: push maintains min-heap order
-- iheap: decrease moves an element up
-- iheap: decrease also handles a raise (a general update, not just decrease)
-- iheap: remove takes a specific element out early
-- iheap: pops in sorted order
-- lru: empty
-- lru: put and get
-- lru: over capacity evicts the least recently used
-- lru: remove
-- lru: capacity 0 means unbounded
-- rbtree: empty
-- rbtree: insert in scrambled order keeps sorted traversal and every invariant
-- rbtree: find
-- rbtree: remove the first half in scrambled order, invariants hold throughout
-- rbtree: remove the rest, ending empty
-- omap: empty
-- omap: set keeps sorted order
-- omap: replace does not add a second entry
-- omap: remove
-- oset: add, has, remove, ordered walk
-- skiplist: empty
-- skiplist: insert in scrambled order keeps sorted traversal
-- skiplist: find
-- skiplist: remove half, in scrambled order
-- skiplist: removing an absent key reports it, twice in a row
-- trie: empty
-- trie: insert and find distinguishes a prefix from a whole word
-- trie: has_prefix
-- trie: each_prefix visits exactly the matching words, children sorted
-- trie: clear resets everything, and the pool is reusable afterward
-- trie: insert fails cleanly once the pool is full
-- generic: DS_DEFAULT_HASH/EQ picks the right pair for hset - int, unsigned int
-- generic: DS_DEFAULT_HASH/EQ with gmap - string and float keys
-- generic: DS_DEFAULT_CMP with omap and flatmap - int and string keys
-- generic: DS_DEFAULT_CMP with iheap - float priorities
-- nothing leaked
ALL PASSED 4288/4288
